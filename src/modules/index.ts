import * as Link from "./Link";
import * as Profile from "./Profile";
import * as RememberItems from "./RememberItems";
import * as User from "./User";
// This object is passed through to the utils folder to generate schema
export const typedefs = {
  Link,
  User,
  Profile,
  RememberItems
};
